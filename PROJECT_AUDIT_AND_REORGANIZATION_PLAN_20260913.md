# Аудит папки проекта и план систематизации

Дата аудита: 2026-09-13
Статус: аудит завершён; минимальная очистка и перенос скриптов выполнены
2026-09-13. Перенос документации и локальных evidence отложен до отдельной
проверки ссылок и состава публикационного набора.

## 1. Фактическое состояние

- Рабочее дерево: 1 440 файлов, около 138,4 МБ вместе с `.git`.
- Основной объём: `output/` — 405 файлов и около 100 МБ; `скрины архивов/` —
  288 файлов и около 21,7 МБ; `scripts/modules/` — 86 файлов и около 0,94 МБ.
- Git отслеживает 164 файла, включая 19 элементов `output/` и 35 файлов из
  `скрины архивов/`. Это противоречит назначению текущего `.gitignore`, где
  `output/`, CSV и логи уже объявлены исключёнными.
- В корне находились 23 Python-скрипта, два `.bat`-/скриптовых маршрута и
  несколько поколений исторической документации.
- В `scripts/modules/` смешаны библиотечные модули, скраперы, локальные parsers,
  тесты, конфигурация фильтров и устаревшие копии с другого компьютера.
- `ONTOLOGY.md` создан как локальный каркас; общая онтология не изменялась.

## 2. Предлагаемая структура

```text
project-root/
├── scripts/
│   ├── scripts/run_search.py                 # основные entry points
│   ├── scripts/check_source.py
│   ├── maintenance/                 # add_*, fix_*, check_*, export/import
│   ├── scripts/modules/                     # рабочие модули и scrapers
│   └── legacy/                      # старые версии и копии, без удаления
├── tests/                           # тесты из modules и новые проверки путей
├── docs/
│   ├── current/                     # актуальные инструкции
│   └── archive/                     # исторические отчёты и checkpoints
├── data/
│   ├── raw/                         # локальные скриншоты и сохранённые HTML
│   └── reference/                   # входные CSV и справочные материалы
├── output/                          # локальные результаты, не публикуются
├── .github/
├── README.md
├── LICENSE
├── requirements.txt
├── AGENTS.md
├── PROGRESS.md
└── DECISIONS.md
```

Перенос выполнен с рефакторингом относительных путей в `scripts/run_search.py`,
`scripts/review.py`, `scripts/catalog_search.py`, `scripts/modules/archive_health.py`
и тестах. Тестовый запуск: 56 пройдено, 9 пропущено из-за отсутствующих
локальных HTML-snapshots.

## 3. Что переносить в папку скриптов

### Основные entry points

`scripts/run_search.py`, `scripts/check_source.py`, `review.py`, `scripts/catalog_search.py`,
`inspect_archives.py`, `run_search_loop.bat`.

### Обслуживание, подготовка и анализ

`add_access_fields.py`, `add_admin_units.py`, `add_all_gubernii.py`,
`add_libraries.py`, `add_sources_fields.py`, `analyze_titles.py`,
`check_all_dbs.py`, `check_missing_fields.py`, `check_notion.py`,
`check_notion2.py`, `create_geoportals_table.py`, `deduplicate_results.py`,
`find_dbs.py`, `fix_titles.py`, `prepare_notion_upload.py`.

### Рабочие модули

Весь текущий `scripts/modules/`, кроме тестов, перенесён в `scripts/modules/` с
сохранением подкаталога `scrapers/`. Тесты вынесены в `tests/`.

## 4. Что сохранять, но убрать из корня

- `скрины архивов/` — исходные пользовательские материалы и evidence; не
  удалять, но вынести в локальный `data/raw/` и исключить из GitHub-публикации;
- `output/` — результаты, логи, manifest, review и резервные копии; не удалять
  целиком, а разделить на актуальные evidence и локальные результаты;
- `kaluga.csv`, `perm.csv` — входные/рабочие выгрузки; не считать кодом и не
  публиковать без отдельной проверки состава и лицензии;
- инструкции и отчёты — переместить в `docs/current/` или `docs/archive/`,
  сохранив даты и provenance.

## 5. Дубли и устаревшие варианты

Следующие файлы имеют имена копий и отличаются по размеру/SHA-256, поэтому их
нельзя считать побайтными дублями и удалять автоматически:

- два `run_search (копия с компьютера POOH-HUAWEI)*.py`;
- `scripts/modules/archive_health (копия с компьютера POOH-HUAWEI).py`;
- `scripts/modules/searcher_libraries (копия с компьютера POOH-HUAWEI).py`;
- `scripts/modules/searcher_neb (копия с компьютера POOH-HUAWEI).py`;
- `scripts/modules/scrapers/searcher_sverdlovsk (копия с компьютера POOH-HUAWEI).py`.

Копии перемещены в `scripts/legacy/` с сохранением исходных имён. То же
относится к `searcher_garf_fixed.py` и
`searcher_kaluga_fixed.py`: текущий `PROGRESS.md` прямо сохраняет вариант
Калуги как экспериментальный.

## 6. Минимальный список кандидатов на удаление

По подтверждению пользователя удалён следующий минимальный набор очевидных
технических остатков:

1. `.ruff_cache/` — кэш Ruff, 3 файла;
2. `scripts/modules/__pycache__/` — 5 скомпилированных `.pyc`;
3. пустой каталог `output/20260703_175458/`;
4. пустые файлы `output/20260721_130705/review.csv` и
   `output/20260721_201251/review.csv`.

Не включены в этот список: исходные скриншоты и HTML, PDF/MARC, датированные
review-артефакты, резервные копии, CSV-выгрузки, Notion-экспорты и все файлы,
которые могут быть научным evidence. Их можно только архивировать или удалять
по отдельному согласованию после проверки provenance.

## 7. Блокеры публикации на GitHub

### Секреты

Локальный `secret_exposure_audit` обнаружил сигналы жёстко заданного
`TOKEN = ...` в 10 отслеживаемых скриптах: `add_admin_units.py`,
`add_all_gubernii.py`, `add_libraries.py`, `add_sources_fields.py`,
`check_all_dbs.py`, `check_missing_fields.py`, `check_notion.py`,
`check_notion2.py`, `find_dbs.py`, `inspect_archives.py`. Значения не
выводились и в отчёт не записывались. Токены заменены на чтение переменной
окружения; повторный аудит новой структуры завершён со статусом `ok`, 0
findings и 0 warnings.

### Состав репозитория

- В Git уже попали часть `output/` и локальных скриншотов, несмотря на
  `.gitignore`; одной правки `.gitignore` для их удаления из будущей публикации
  недостаточно.
- В `скрины архивов/` есть сохранённые служебные ресурсы, включая большой
  JavaScript/JSON-файл с token-подобным полем. Его не следует публиковать без
  отдельного решения о необходимости и очистке.
- `.github/workflows/search.yml` переведён в read-only режим с
  `permissions: contents: read`; Notion-токен и commit/push результатов удалены.
- `README.md` переписан по актуальным ограничениям и новой структуре.
- `requirements.txt` дополнен `python-dotenv` и Playwright; установка Chromium
  остаётся локальной операцией и в CI не выполняется.

## 8. Порядок безопасного приведения к публикации

1. Подтвердить минимальный список удаления выше — выполнено.
2. Удалить только технические кэши и пустые результаты — выполнено.
3. Перенести код в `scripts/`, тесты — в `tests/`, сохранив legacy-копии — выполнено.
4. Исправить относительные пути в коде и актуальном README — выполнено.
5. Перенести документацию и исходные материалы в `docs/` и `data/`, не удаляя
   evidence.
6. Убрать hardcoded tokens, проверить Git-индекс и пересмотреть workflow —
   токены и workflow выполнены; проверка индекса ожидает отдельного Git-разрешения.
7. Добавить smoke-тесты импорта, `--help`, dry-run и локальных fixtures —
   тесты и `--help` выполнены, dry-run требует Notion-конфигурации.
8. После проверки подготовить отдельный публикуемый Git-слой; `git push` не
   выполнять без отдельного разрешения.

Публикация пока не выполняется: локальные материалы уже помечены в `.gitignore`,
но ранее отслеживаемые `output/` и `скрины архивов/` нужно удалить из Git-индекса
отдельной операцией после проверки состава и получения сессионного Git-разрешения.
