# ✅ ЧЕКЛИСТ: Статус архивов (обновлён 2026-07-18)

## 🟢 ГОТОВЫЕ (5) — Запустить в полном поиске

- [x] **ГПИБ** — Государственная публичная историческая библиотека
  - Метод: WebDriver (Selenium)
  - Скрипт: `searcher_gpib.py` ✅
  - Статус: **ГОТОВ, протестирован на 49 территориях**
  - Комбинации: 5 × 49 = 245

- [x] **ГАЯО** — Государственный архив Ярославской области
  - Метод: GET (морфологический поиск)
  - Скрипт: `searcher_gayao.py` ✅
  - Статус: **ГОТОВ, протестирован на 49 территориях**
  - Комбинации: 5 × 49 = 245

- [x] **РНБ** — Каталог печатных карт XVIII века
  - Метод: POST (windows-1251 форма)
  - Скрипт: `searcher_rnb_kart.py` ✅
  - Статус: **ГОТОВ, протестирован на 49 территориях**
  - Комбинации: 49 (все уезды)

- [x] **НЭБ** — Национальная электронная библиотека
  - Метод: GET (с retry для таймаутов)
  - Скрипт: `searcher_neb.py` ✅
  - Статус: **ГОТОВ, retry logic добавлен, протестирован**
  - Комбинации: 5 × 49 = 245

- [x] **ГАПК archive1** — Архивы Прикамья (раздел archive1)
  - Метод: GET фильтра по ключевым словам
  - Скрипт: `searcher_gapk.py` ✅
  - Статус: **ГОТОВ, протестирован на 49 территориях**
  - Комбинации: 7 ключей × 49 = 343

**ИТОГО ГОТОВЫХ КОМБИНАЦИЙ: 980**

---

## 🟡 НЕДОРАБОТАННЫЕ (7) — ЧЕКПОЙНТЫ PARTIAL СКРИПТОВ

### 1️⃣ ГАРФ (frameset/GET) — `searcher_garf_fixed.py` ⚠️

**Чекпойнт текущего состояния (2026-07-18):**
```python
# Статус: GET-запрос работает, селекторы нужно уточнить
BASE_URL = "https://www.fgurgia.ru"
SEARCH_URL = f"{BASE_URL}/search"
# URL шаблон:
# https://www.fgurgia.ru/search?p0=v-карта&type=simple&p0_c=12
# p0_c=12 = код для документов типа "карта"
```

**ЧТО НЕ ГОТОВО:**
- [ ] HTML-селекторы для результатов (не знаем какие `.class` / `tag` используются)
- [ ] Парсинг таблицы результатов (Ф., Оп., Д., года)

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Запустить: `python modules/scrapers/searcher_garf_fixed.py "карта" --debug`
2. Посмотреть HTML выход (первые 5000 символов)
3. Открыть https://www.fgurgia.ru/search?p0=v-карта&type=simple&p0_c=12 в браузере
4. DevTools → Elements → найти селектор для каждой ячейки результата
5. Обновить строки:
   ```python
   results_section = soup.find("table", class_="???")  # или другой селектор
   rows = results_section.find_all("tr")  # или "td"
   ```

---

### 2️⃣ Калужская ОНБИБ (Веб-ИРБИС 64) — `searcher_kaluga_fixed.py` ⚠️

**Чекпойнт текущего состояния (2026-07-18):**
```python
# Статус: POST-шаблон готов, параметры не проверены
BASE_URL = "https://ibald.ru"
CGI_URL = f"{BASE_URL}/cgi-bin/irbis64r_99"

data = {
    "database": "ibald",           # ← УТОЧНИТЬ
    "searchtype": "I",             # ← УТОЧНИТЬ (I/K/S/T)
    "query": query,
    "format": "HTML",              # ← УТОЧНИТЬ
}
resp = requests.post(CGI_URL, data=data)
resp.encoding = "windows-1251"
```

**ЧТО НЕ ГОТОВО:**
- [ ] Параметр `database` (проверить в Калуге какое имя БД)
- [ ] Параметр `searchtype` (I = информационный поиск? K = ключевые слова?)
- [ ] Параметр `format` (что возвращает: HTML, JSON, XML?)
- [ ] CGI URL (правильный ли /cgi-bin/irbis64r_99?)

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://ibald.ru в браузере
2. Выполнить поиск "карта Калужская"
3. DevTools → Network → найти POST запрос
4. Скопировать параметры из Request Body
5. Обновить значения в коде
6. Тестировать: `python modules/scrapers/searcher_kaluga_fixed.py "карта"`

---

### 3️⃣ Пермская краевая ОНБИБ (ELiS CMS) — `searcher_permkrai_fixed.py` ⚠️

**Чекпойнт состояния (2026-07-18):**
```python
# Статус: Skeleton (основной код не написан)
BASE_URL = "https://permlib.ru"
# Проблема: ELiS CMS загружает результаты через AJAX
# Нужно использовать Playwright с wait_for_load_state("networkidle")
```

**ЧТО НУЖНО:**
- [ ] Выяснить структуру AJAX запроса (URL, параметры)
- [ ] Или использовать Playwright для auto-waiting
- [ ] Парсить результаты из DOM после загрузки

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://permlib.ru в браузере
2. Найти форму поиска → выполнить поиск "карта"
3. DevTools → Network → XHR → найти AJAX запрос
4. Если JSON API — использовать `requests.get()`
5. Если только DOM — использовать Playwright

---

### 4️⃣ Смоленская ОУНБ — `searcher_smolensk_fixed.py` ⚠️

**Чекпойнт состояния (2026-07-18):**
```python
# Статус: Skeleton (основной код не написан)
BASE_URL = "https://smolbnlib.ru"
# Проблема: Нет встроенного поиска, только walk по каталогу
# Стратегия: Рекурсивный обход Фонды → Коллекции → Документы
```

**ЧТО НУЖНО:**
- [ ] Найти структуру каталога (URL паттерны)
- [ ] Реализовать рекурсивный обход страниц
- [ ] Фильтр по названию документа (содержит "карта")

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://smolbnlib.ru
2. Найти раздел "Фонды" или "Каталог"
3. Посмотреть структуру навигации (как устроены ссылки)
4. Парсить ссылки, рекурсивно обходить

---

### 5️⃣ РГБ (Российская государственная библиотека) — `searcher_rgb_fixed.py` ⚠️

**Чекпойнт состояния (2026-07-18):**
```python
# Статус: Skeleton (основной код не написан)
BASE_URL = "https://www.rsl.ru/ru/s97/s339/"
# Проблема: POST API требует CSRF-токена из cookie
# Техника: requests.Session() + обработка CSRF
```

**ЧТО НУЖНО:**
- [ ] Извлечь CSRF-токен со страницы поиска
- [ ] Сохранить session cookies
- [ ] Отправить POST с правильными параметрами

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://www.rsl.ru/ru/s97/s339/
2. DevTools → Network → отправить поиск
3. Посмотреть POST параметры и CSRF-токен
4. Написать код с Session

---

### 6️⃣ ЦГА Москвы (Bitrix CMS) — `searcher_cga_moskva_fixed.py` ⚠️

**Чекпойнт состояния (2026-07-18):**
```python
# Статус: Skeleton (основной код не написан)
BASE_URL = "https://www.tica.ru"
# Проблема: Bitrix CMS с динамической загрузкой результатов
# Техника: Selenium/Playwright для поиска селекторов
```

**ЧТО НУЖНО:**
- [ ] Найти правильные CSS-селекторы для результатов
- [ ] Использовать Selenium для поиска элементов

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://www.tica.ru в браузере
2. Выполнить поиск "карта"
3. DevTools → Elements → найти `.class` результатов
4. Обновить селекторы в коде

---

### 7️⃣ ГАКО (Государственный архив Калужской области) — `searcher_gako_fixed.py` ⚠️

**Чекпойнт состояния (2026-07-18):**
```python
# Статус: Skeleton (основной код не написан)
BASE_URL = "https://gako.ru"
# Проблема: AJAX-результаты, нужен JSON API или Playwright
# Техника: DevTools анализ XHR запросов
```

**ЧТО НУЖНО:**
- [ ] Найти XHR запрос для результатов
- [ ] Если JSON API — использовать requests
- [ ] Если только DOM — использовать Playwright

**СЛЕДУЮЩИЙ ШАГ БЕЗ VPN:**
1. Открыть https://gako.ru
2. DevTools → Network → выполнить поиск "карта"
3. Посмотреть XHR запросы (найти JSON ответ)
4. Скопировать URL и параметры

---

## ❌ НЕВОЗМОЖНЫЕ СЕЙЧАС (4)

- [ ] **ГАПК archive** (раздел /archive/) — геоблок для нероссийских IP (запускать БЕЗ VPN)
- [ ] **Росархив** — нет открытого API (требует переговоров)
- [ ] **РГИА** — требует зарегистрированного доступа
- [ ] **РГВИА** — только справочная служба (письмо/телефон)

---

## 📊 СТАТИСТИКА

| Статус | Кол-во | Действие |
|--------|--------|----------|
| ✅ Готово | 5 | Запустить сейчас |
| ⚠️ Partial | 7 | Доработать (чекпойнты выше) |
| ❌ Невозможно | 4 | Отложить или запросить доступ |
| **ИТОГО** | **16** | |

